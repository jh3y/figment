packages:
  - apps/*
  - packages/*

# sharp resizes gallery art. Prebuilt binaries cover the common platforms, but pnpm 10 blocks
# install scripts by default, and a fresh clone should not have to answer a prompt about it.
onlyBuiltDependencies:
  - sharp
