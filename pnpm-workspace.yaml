packages:
  - apps/*
  - packages/*

# pnpm 10 blocks dependency install scripts by default, and a fresh clone should not open with a
# prompt about them. sharp resizes gallery art; esbuild is Vite's bundler and predates it here.
onlyBuiltDependencies:
  - esbuild
  - sharp
